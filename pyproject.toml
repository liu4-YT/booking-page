[project]
name = "booking-page"
version = "0.1.0"
description = "Booking API"
requires-python = ">=3.11"
dependencies = [
    "flask==3.0.0"
]

[tool.vercel]
entrypoint = "api.booking:app"
